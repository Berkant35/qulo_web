/// the duration until the player notices the color
const Duration sightDuration = Duration(milliseconds: 300);

const Duration defaultGameDuration = Duration(seconds: 30);
const Duration defaultTimeOutDuration = Duration(seconds: 3);
const int defaultGameDistanceMM = 300; // 30 cm
