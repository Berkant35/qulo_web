//This enumaration help to you for control record states.
// For example if you want when recording something you can use if-> recording state -> bla bla

enum RecordStates {
  recording,
  stopped,
  pause,
  ready,
  idle}
