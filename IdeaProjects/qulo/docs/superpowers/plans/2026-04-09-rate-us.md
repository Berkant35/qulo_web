# Rate Us / App Review — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Match kutlaması sonrası native in-app review dialog'u göster + Settings'e kalıcı "Rate Us" butonu ekle.

**Architecture:** Singleton `AppReviewManager` (Hardware Manager Pattern) — `in_app_review` paketini sarar, SharedPreferences ile cooldown/sayaç yönetir. `CelebrationButtons` callback'lerinde tetiklenir, `SettingsScreen`'de kalıcı tile olarak yer alır.

**Tech Stack:** `in_app_review` (native dialog), `shared_preferences` (mevcut), `url_launcher` (mevcut fallback), Firebase Analytics (mevcut)

---

## File Map

| Dosya | Aksiyon | Sorumluluk |
|-------|---------|------------|
| `pubspec.yaml` | Modify:62 | `in_app_review` paketi ekle |
| `lib/core/services/app_review_manager.dart` | Create | Singleton manager — review state + native dialog |
| `lib/core/services/analytics_events.dart` | Modify:230 | 3 yeni event constant |
| `lib/core/services/analytics_manager.dart` | Modify:225 | 3 yeni helper method |
| `lib/features/quiz/mixins/quiz_screen_mixin.dart` | Modify:357-364 | `onStartChat` / `onGoBack` sonrası review tetikleme |
| `lib/features/settings/screens/settings_screen.dart` | Modify:54-58 | Rate Us tile ekleme (Legal section sonrası) |
| `lib/core/l10n/translations/en.dart` | Modify:1012 | 2 yeni string |
| `lib/core/l10n/translations/tr.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/de.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/fr.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/es.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/it.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/pt.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/pl.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/ru.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/ko.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/ja.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/zh.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/ar.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/hi.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/sv.dart` | Modify | 2 yeni string |
| `lib/core/l10n/translations/nl.dart` | Modify | 2 yeni string |

---

### Task 1: `in_app_review` paketini ekle

**Files:**
- Modify: `qulov2/pubspec.yaml:62`

- [ ] **Step 1: pubspec.yaml'a paketi ekle**

`url_launcher: ^6.3.1` satırının altına ekle:

```yaml
  in_app_review: ^2.0.10
```

- [ ] **Step 2: Bağımlılıkları indir**

Run: `cd /Users/berkantcalikusu/IdeaProjects/qulo/qulov2 && flutter pub get`
Expected: `Got dependencies!` mesajı

- [ ] **Step 3: Commit**

```bash
git add qulov2/pubspec.yaml qulov2/pubspec.lock
git commit -m "chore: add in_app_review package for native store review"
```

---

### Task 2: Analytics event'lerini ekle

**Files:**
- Modify: `qulov2/lib/core/services/analytics_events.dart:230`
- Modify: `qulov2/lib/core/services/analytics_manager.dart:225`

- [ ] **Step 1: AnalyticsEvents'e yeni constant'lar ekle**

`analytics_events.dart` dosyasında `// ─── Settings (8)` bölümünün hemen üstüne (line 230) ekle:

```dart
  // ─── App Review (3) ──────────────────────────────────────────────
  static const String appReviewPrompted = 'app_review_prompted';
  static const String appReviewDismissed = 'app_review_dismissed';
  static const String appReviewCompleted = 'app_review_completed';
```

Aynı dosyada parameter keys bölümüne (`paramLanguageCount` satırından sonra) ekle:

```dart
  static const String paramShownCount = 'shown_count';
```

- [ ] **Step 2: AnalyticsManager'a helper method'lar ekle**

`analytics_manager.dart` dosyasında `logDeepLinkInvalid` method'unun kapanış `}` satırından sonra ekle:

```dart
  // ── App Review Analytics ──────────────────────────────────────────

  void logAppReviewPrompted(String trigger, int shownCount) {
    logEvent(AnalyticsEvents.appReviewPrompted, params: {
      AnalyticsEvents.paramTrigger: trigger,
      AnalyticsEvents.paramShownCount: shownCount,
    });
  }

  void logAppReviewDismissed(String trigger) {
    logEvent(AnalyticsEvents.appReviewDismissed, params: {
      AnalyticsEvents.paramTrigger: trigger,
    });
  }

  void logAppReviewCompleted(String trigger) {
    logEvent(AnalyticsEvents.appReviewCompleted, params: {
      AnalyticsEvents.paramTrigger: trigger,
    });
  }
```

- [ ] **Step 3: Analyze kontrolü**

Run: `cd /Users/berkantcalikusu/IdeaProjects/qulo/qulov2 && flutter analyze lib/core/services/analytics_events.dart lib/core/services/analytics_manager.dart`
Expected: No issues found

- [ ] **Step 4: Commit**

```bash
git add qulov2/lib/core/services/analytics_events.dart qulov2/lib/core/services/analytics_manager.dart
git commit -m "feat: add app review analytics events and helper methods"
```

---

### Task 3: AppReviewManager singleton oluştur

**Files:**
- Create: `qulov2/lib/core/services/app_review_manager.dart`

- [ ] **Step 1: AppReviewManager dosyasını oluştur**

```dart
import 'dart:io';

import 'package:flutter/foundation.dart';
import 'package:in_app_review/in_app_review.dart';
import 'package:shared_preferences/shared_preferences.dart';
import 'package:qulo_v2/core/services/analytics_manager.dart';
import 'package:qulo_v2/core/services/url_launcher_manager.dart';

class AppReviewManager {
  AppReviewManager._();
  static final AppReviewManager instance = AppReviewManager._();

  static const _keyShownCount = 'qulo_app_review_shown_count';
  static const _keyLastShown = 'qulo_app_review_last_shown';
  static const _keyCompleted = 'qulo_app_review_completed';

  static const _maxShownCount = 3;
  static const _cooldownDays = 14;

  // iOS App Store URL — Android uses applicationId automatically
  static const _iosAppStoreUrl =
      'https://apps.apple.com/app/id<APP_STORE_ID>';
  static const _androidPlayStoreUrl =
      'https://play.google.com/store/apps/details?id=com.wordpress.calikusuberkant.qulo';

  final InAppReview _inAppReview = InAppReview.instance;

  /// Smart trigger — checks cooldown, count, and platform availability.
  Future<void> tryShowReview({required String trigger}) async {
    try {
      if (!await _shouldShowReview()) return;
      await _requestReview(trigger);
    } catch (e, stack) {
      AnalyticsManager.instance.logNonFatalError(
        e,
        stack,
        context: 'AppReviewManager.tryShowReview',
      );
    }
  }

  /// Settings tap — no cooldown check, with store URL fallback.
  Future<void> requestReviewFromSettings() async {
    try {
      final available = await _inAppReview.isAvailable();
      if (available) {
        await _inAppReview.requestReview();
        AnalyticsManager.instance.logAppReviewPrompted('settings', 0);
      } else {
        await _openStoreUrl();
      }
    } catch (e, stack) {
      // Fallback to store URL on any error
      await _openStoreUrl();
      AnalyticsManager.instance.logNonFatalError(
        e,
        stack,
        context: 'AppReviewManager.requestReviewFromSettings',
      );
    }
  }

  Future<bool> _shouldShowReview() async {
    final prefs = await SharedPreferences.getInstance();

    final completed = prefs.getBool(_keyCompleted) ?? false;
    if (completed) return false;

    final shownCount = prefs.getInt(_keyShownCount) ?? 0;
    if (shownCount >= _maxShownCount) {
      await prefs.setBool(_keyCompleted, true);
      return false;
    }

    final lastShownStr = prefs.getString(_keyLastShown);
    if (lastShownStr != null) {
      final lastShown = DateTime.tryParse(lastShownStr);
      if (lastShown != null) {
        final daysSince = DateTime.now().difference(lastShown).inDays;
        if (daysSince < _cooldownDays) return false;
      }
    }

    final available = await _inAppReview.isAvailable();
    if (!available) return false;

    return true;
  }

  Future<void> _requestReview(String trigger) async {
    final prefs = await SharedPreferences.getInstance();
    final shownCount = (prefs.getInt(_keyShownCount) ?? 0) + 1;

    await _inAppReview.requestReview();

    // Update state
    await prefs.setInt(_keyShownCount, shownCount);
    await prefs.setString(_keyLastShown, DateTime.now().toIso8601String());

    if (shownCount >= _maxShownCount) {
      await prefs.setBool(_keyCompleted, true);
    }

    AnalyticsManager.instance.logAppReviewPrompted(trigger, shownCount);

    if (kDebugMode) {
      debugPrint('[AppReviewManager] Review prompted (trigger: $trigger, count: $shownCount)');
    }
  }

  Future<void> _openStoreUrl() async {
    final url = Platform.isIOS ? _iosAppStoreUrl : _androidPlayStoreUrl;
    await UrlLauncherManager.instance.launch(url);
  }
}
```

- [ ] **Step 2: Analyze kontrolü**

Run: `cd /Users/berkantcalikusu/IdeaProjects/qulo/qulov2 && flutter analyze lib/core/services/app_review_manager.dart`
Expected: No issues found

- [ ] **Step 3: Commit**

```bash
git add qulov2/lib/core/services/app_review_manager.dart
git commit -m "feat: add AppReviewManager singleton with smart trigger logic"
```

---

### Task 4: Match kutlaması sonrası review tetikleme

**Files:**
- Modify: `qulov2/lib/features/quiz/mixins/quiz_screen_mixin.dart:357-364`

- [ ] **Step 1: Import ekle**

`quiz_screen_mixin.dart` dosyasının import bölümüne ekle:

```dart
import 'package:qulo_v2/core/services/app_review_manager.dart';
```

- [ ] **Step 2: onStartChat ve onGoBack method'larını güncelle**

Mevcut `onStartChat` method'unu bul ve değiştir:

```dart
  void onStartChat() {
    ref.invalidate(matchListProvider);
    ref.read(navigationServiceProvider).go(RouteNames.matches);
    AppReviewManager.instance.tryShowReview(trigger: 'match_celebration');
  }

  void onGoBack() {
    ref.read(navigationServiceProvider).go(RouteNames.discover);
    AppReviewManager.instance.tryShowReview(trigger: 'match_celebration');
  }
```

Navigasyon ÖNCE yapılır, review overlay olarak üstüne gelir.

- [ ] **Step 3: Analyze kontrolü**

Run: `cd /Users/berkantcalikusu/IdeaProjects/qulo/qulov2 && flutter analyze lib/features/quiz/mixins/quiz_screen_mixin.dart`
Expected: No issues found

- [ ] **Step 4: Commit**

```bash
git add qulov2/lib/features/quiz/mixins/quiz_screen_mixin.dart
git commit -m "feat: trigger app review after match celebration navigation"
```

---

### Task 5: Settings ekranına Rate Us tile ekle

**Files:**
- Modify: `qulov2/lib/features/settings/screens/settings_screen.dart:1,54-58`

- [ ] **Step 1: Import ekle**

`settings_screen.dart` dosyasının import bölümüne ekle:

```dart
import 'package:qulo_v2/core/services/app_review_manager.dart';
```

- [ ] **Step 2: Rate Us tile ekle**

`settings_screen.dart`'ta `SettingsLegalSection` widget'ından sonraki `const SizedBox(height: AppSpacing.sm)` satırından ÖNCE, `SettingsLegalSection` widget'ının hemen altına ekle:

Mevcut:
```dart
          SettingsLegalSection(
            onTerms: onOpenTerms,
            onPrivacy: onOpenPrivacy,
          ),
          const SizedBox(height: AppSpacing.sm),
```

Yeni:
```dart
          SettingsLegalSection(
            onTerms: onOpenTerms,
            onPrivacy: onOpenPrivacy,
          ),
          SettingsActionTile(
            icon: Icons.star_rate_rounded,
            title: context.tr('rate_us'),
            onTap: () async {
              await AppReviewManager.instance.requestReviewFromSettings();
            },
          ),
          const SizedBox(height: AppSpacing.sm),
```

- [ ] **Step 3: Analyze kontrolü**

Run: `cd /Users/berkantcalikusu/IdeaProjects/qulo/qulov2 && flutter analyze lib/features/settings/screens/settings_screen.dart`
Expected: No issues found

- [ ] **Step 4: Commit**

```bash
git add qulov2/lib/features/settings/screens/settings_screen.dart
git commit -m "feat: add Rate Us tile to settings screen below legal section"
```

---

### Task 6: i18n — tüm 16 dile çeviri ekle

**Files:**
- Modify: Tüm `qulov2/lib/core/l10n/translations/*.dart` dosyaları (16 dosya)

Her dosyada, kapanış `};` satırından HEMEN ÖNCE, son string entry'den sonra aşağıdaki string'leri ekle. Arada bir boş satır ile section başlığı koy.

- [ ] **Step 1: en.dart**

`'profile_completion_title': 'Complete Your Profile',` satırından sonra ekle:

```dart

  // App Review
  'rate_us': 'Rate Us',
  'rate_us_subtitle': 'Love Qulo? Leave us a review!',
```

- [ ] **Step 2: tr.dart**

Son string entry'den sonra ekle:

```dart

  // App Review
  'rate_us': 'Bizi Değerlendir',
  'rate_us_subtitle': 'Qulo\'yu seviyor musun? Bize yorum bırak!',
```

- [ ] **Step 3: de.dart**

```dart

  // App Review
  'rate_us': 'Bewerte uns',
  'rate_us_subtitle': 'Liebst du Qulo? Hinterlasse uns eine Bewertung!',
```

- [ ] **Step 4: fr.dart**

```dart

  // App Review
  'rate_us': 'Évaluez-nous',
  'rate_us_subtitle': 'Vous aimez Qulo ? Laissez-nous un avis !',
```

- [ ] **Step 5: es.dart**

```dart

  // App Review
  'rate_us': 'Califícanos',
  'rate_us_subtitle': '¿Te gusta Qulo? ¡Déjanos una reseña!',
```

- [ ] **Step 6: it.dart**

```dart

  // App Review
  'rate_us': 'Valutaci',
  'rate_us_subtitle': 'Ti piace Qulo? Lasciaci una recensione!',
```

- [ ] **Step 7: pt.dart**

```dart

  // App Review
  'rate_us': 'Avalie-nos',
  'rate_us_subtitle': 'Gosta do Qulo? Deixe-nos uma avaliação!',
```

- [ ] **Step 8: pl.dart**

```dart

  // App Review
  'rate_us': 'Oceń nas',
  'rate_us_subtitle': 'Lubisz Qulo? Zostaw nam opinię!',
```

- [ ] **Step 9: ru.dart**

```dart

  // App Review
  'rate_us': 'Оцените нас',
  'rate_us_subtitle': 'Нравится Qulo? Оставьте отзыв!',
```

- [ ] **Step 10: ko.dart**

```dart

  // App Review
  'rate_us': '평가하기',
  'rate_us_subtitle': 'Qulo가 마음에 드시나요? 리뷰를 남겨주세요!',
```

- [ ] **Step 11: ja.dart**

```dart

  // App Review
  'rate_us': '評価する',
  'rate_us_subtitle': 'Quloが気に入りましたか？レビューを書いてください！',
```

- [ ] **Step 12: zh.dart**

```dart

  // App Review
  'rate_us': '评价我们',
  'rate_us_subtitle': '喜欢Qulo吗？给我们留下评价吧！',
```

- [ ] **Step 13: ar.dart**

```dart

  // App Review
  'rate_us': 'قيّمنا',
  'rate_us_subtitle': 'هل تحب Qulo؟ اترك لنا تقييمًا!',
```

- [ ] **Step 14: hi.dart**

```dart

  // App Review
  'rate_us': 'हमें रेट करें',
  'rate_us_subtitle': 'क्या आपको Qulo पसंद है? हमें रिव्यू दें!',
```

- [ ] **Step 15: sv.dart**

```dart

  // App Review
  'rate_us': 'Betygsätt oss',
  'rate_us_subtitle': 'Gillar du Qulo? Lämna en recension!',
```

- [ ] **Step 16: nl.dart**

```dart

  // App Review
  'rate_us': 'Beoordeel ons',
  'rate_us_subtitle': 'Vind je Qulo leuk? Laat een review achter!',
```

- [ ] **Step 17: Analyze kontrolü**

Run: `cd /Users/berkantcalikusu/IdeaProjects/qulo/qulov2 && flutter analyze lib/core/l10n/translations/`
Expected: No issues found

- [ ] **Step 18: Commit**

```bash
git add qulov2/lib/core/l10n/translations/
git commit -m "feat: add rate_us and rate_us_subtitle strings for all 16 languages"
```

---

### Task 7: Tam entegrasyon testi + flutter analyze

**Files:** Tüm değiştirilen dosyalar

- [ ] **Step 1: Tam proje analyze**

Run: `cd /Users/berkantcalikusu/IdeaProjects/qulo/qulov2 && flutter analyze`
Expected: No issues found

- [ ] **Step 2: Build kontrolü (iOS)**

Run: `cd /Users/berkantcalikusu/IdeaProjects/qulo/qulov2 && flutter build ios --no-codesign --debug 2>&1 | tail -5`
Expected: Build successful

- [ ] **Step 3: Eğer hata varsa düzelt ve tekrar analyze et**

Hataları düzelt, commit et.

---

## Notlar

- `_iosAppStoreUrl` içindeki `<APP_STORE_ID>` — app store'a yüklendiğinde gerçek ID ile değiştirilecek. Şimdilik placeholder. Settings fallback'te kullanılır.
- `rate_us_subtitle` şu an kullanılmıyor ama ileride Settings tile'a subtitle eklemek istenirse hazır.
- `in_app_review` paketi debug mode'da simülatörde çalışmaz — test için TestFlight/Internal Testing gerekir.
